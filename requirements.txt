# --- Web layer ---
fastapi>=0.138.1
uvicorn[standard]>=0.49.0
streamlit>=1.58.0
requests>=2.34.2
pydantic>=2.13.4

# --- LangChain core + ecosystem ---
langchain>=1.3.11
langchain-core>=1.4.8
langchain-classic>=1.0.8        # EnsembleRetriever now lives here, not in `langchain`
langchain-community>=0.4.2      # BM25Retriever, FAISS vectorstore wrapper
langchain-huggingface>=1.2.2    # HuggingFaceEmbeddings
langchain-text-splitters>=1.1.2 # RecursiveCharacterTextSplitter

# --- Agents / memory ---
langgraph>=1.2.6                # InMemorySaver checkpointer, used by create_agent

# --- Retrieval backends ---
faiss-cpu>=1.14.3               # vector store (use faiss-gpu instead if you have CUDA)
rank-bm25>=0.2.2                # backs langchain_community.retrievers.BM25Retriever
sentence-transformers>=5.6.0    # backend for all-MiniLM-L6-v2 embeddings

# --- LLM provider ---
groq>=0.30.0,<1.0.0             # langchain-groq (all releases incl. 1.1.3) requires groq<1.0.0
langchain-groq>=1.1.3           # required by init_chat_model("groq:...") under the hood